const enum Endpoints {
  root = '/',
  signup = '/signup',
  signin = '/signin',
  signout = '/signout',
  current = '/current',
  profile = '/profile',
  avatars = '/avatars',
  monthly = '/monthly',
  dynamicId = 'dynamicId',
}

export default Endpoints;
