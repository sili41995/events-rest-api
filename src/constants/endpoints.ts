const enum Endpoints {
  root = '/',
  signup = '/signup',
  signin = '/signin',
  signout = '/signout',
  current = '/current',
  profile = '/profile',
  avatars = '/avatars',
}

export default Endpoints;
